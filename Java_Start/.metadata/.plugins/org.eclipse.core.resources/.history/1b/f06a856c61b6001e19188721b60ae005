
public class PersonTest {
	public static void main(String[] args) {
		// 클래스로 객체 생성
		// Person 클래스를 변수의 타입으로 사용
		// => 클래스는 사용자 정의 자료형
		
		// 객체 생성
		// 클래스명 변수명 = new 클래스명();
		
		Person p = new Person();
		
		// yang : instance
		// 객체의 멤버변수에 접근할 때는 .연산자 사용
		// . 가지고 있는
		
		p.name = "Yang";
		p.age = 27;
		p.hobby = "Play";
		
		p.info();
		System.out.println("이름은 " + p.name + "이고, 나이는 " + p.age + "새 취미는 " + p.hobby + "입니다.");
		Person.hello(); // 객체 생성없이 클래스 이름으로 바로 접근
		
	}
}
